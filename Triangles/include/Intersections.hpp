#pragma once

#include "Config.hpp"
#include "Vector.hpp"
#include "Geometry.hpp"


namespace triangles
{

template<typename T>
class Intersection
{
    public:

        static std::pair<Vector<T>, bool> intersects(const Line<T>& l1, const Line<T>& l2)
            {
                
            }
};

} // namespace triangles
